#pragma once

namespace HelperFunctions {
    bool isPrime(int number);
    
    int reverseNumber(int number);

    bool isPalindrome(int number);

    bool isPowOfTwo(int n);

    bool containsOnlyDistinctDigits(int number);
};